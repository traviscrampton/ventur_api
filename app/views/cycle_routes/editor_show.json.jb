json = {
  cycleRoute: {
    id: @cycle_route.id,
    initialRegion: {
      latitude: @cycle_route.latitude,
      longitude: @cycle_route.longitude,
      longitudeDelta: @cycle_route.longitude_delta,
      latitudeDelta: @cycle_route.longitude_delta
    },
    polylines: @cycle_route.polylines,
    includedActivities: @cycle_route.included_activities,
    previousPolylines: previous_polylines
  }
}

json