json = {
  gearItems: @gear_items.map do |gear_item|
    {
      id: gear_item.id,
      name: gear_item.name,
      imageUrl: gear_item.image_url,
      verified: gear_item.verified
    }
  end
}

json